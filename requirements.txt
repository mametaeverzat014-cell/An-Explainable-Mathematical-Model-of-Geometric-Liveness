# Минимальный набор зависимостей (только CPU, без облачных API).
#
# Поддерживаются Python 3.10-3.12 и 3.13+ (разными ветками mediapipe, см. ниже).
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
pyyaml>=6.0
opencv-python>=4.8
# Версия mediapipe выбирается автоматически по версии Python:
#   * Python 3.10-3.12 -> ветка 0.10.x: модель FaceMesh ВСТРОЕНА, ничего качать не надо;
#   * Python 3.13+     -> ветка 1.x: solutions.face_mesh удалён, нужен локальный файл
#     модели face_landmarker.task (одна команда, см. README, раздел 3.3).
mediapipe>=0.10,<1.0; python_version < "3.13"
mediapipe>=1.0; python_version >= "3.13"
# Для тестов (необязательно для запуска пайплайна):
pytest>=7.4
